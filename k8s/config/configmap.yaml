apiVersion: v1
kind: ConfigMap

metadata:
  name: pasal-config
  namespace: pasal

data:
  DATABASE_URL: "postgresql://postgres:postgres@postgres:5432/pasal_db"
  REDIS_URL: "redis://redis:6379/0"
  ACCESS_TOKEN_EXPIRE_MINUTES: "60"
